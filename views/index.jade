extends layout

block content
    .container
        .row
            .col-sm-8
                ul.list-group
                    each article in articles[0]
                        li.list-group-item #{article.name}
            .col-sm-4
                ul.list-group
                    each category in categories[0]
                        li.list-group-item=category.name